namespace ExceptionsDelegatesEvents
{
    /// <summary>
    /// Direction is an enum used for storing the direction of the car.
    /// </summary>
    public enum Direction
    {
        /// <summary>
        /// Used for storing Straight direction.
        /// </summary>
        Straight = 1,

        /// <summary>
        /// Used for storing Left direction.
        /// </summary>
        Left = 2,

        /// <summary>
        /// Used for storing Right direction.
        /// </summary>
        Right = 3,
    }
}
